{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for sparkhacks2024",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "prettier . --write && nodemon index.js",
    "format": "prettier src --check",
    "format-write": "prettier src --write"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemon": "^3.0.3"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
